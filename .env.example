# Geen API key nodig — Claude Agent SDK gebruikt Claude Code CLI auth
# Zorg dat je bent ingelogd: claude login (via Max abonnement)

# Server settings
HOST=0.0.0.0
PORT=8000

# Database
DATABASE_PATH=data/agent_memory.db

# Obsidian vault pad (optioneel — voor project/notitie context)
OBSIDIAN_VAULT_PATH=/Users/jelle/Documents/ObsidianVault
